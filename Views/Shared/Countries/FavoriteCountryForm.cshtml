@model string
@using (Html.BeginForm("FavoriteCountry", "Countries", FormMethod.Post))
{
<label class="w-full">
    <input type="hidden" name="CountryName" maxlength="70" value="@Model"/>
</label>

<button type="submit" class="bg-blue-500 hover:bg-blue-400 text-white font-bold py-2 px-4 border-b-4 border-blue-700 hover:border-blue-500 rounded">
    Favoritar
</button>
}